---
import { FooterSection } from './react/FooterSection';

// 1. AQUI RECIBIMOS LOS DATOS (IGUAL QUE EN HEADER.ASTRO)
const { whatsappNumber, facebookUrl, instagramUrl } = Astro.props;
---

<FooterSection 
    client:visible 
    whatsappNumber={whatsappNumber} 
    facebookUrl={facebookUrl} 
    instagramUrl={instagramUrl} 
/>