---
import { HeaderSection } from './react/HeaderSection';

// Recibimos la prop desde el layout o index
const { whatsappNumber } = Astro.props;
---

<HeaderSection client:load whatsappNumber={whatsappNumber} />