---
import { HowItWorksSection } from './react/HowItWorksSection';

// Recibimos la prop desde el index
const { whatsappNumber } = Astro.props;
---

<HowItWorksSection client:visible whatsappNumber={whatsappNumber} />